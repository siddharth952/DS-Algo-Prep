
//sum = (sum % 10^9 + i % 10^9) % 10^9
//Question: No Minimum No Maximum

#include<iostream>
#include<vector>
using namespace std;

void solve() {
    int n;cin>>n;
    int k;cin>>k;

    vector<int> a(n);
    for (int i = 0; i < n; i++)
       cin >> a[i];
    
    //Sort it as we mult mid

    return noToTell;
}

int main(){
    int T;cin>>T;
    while (T--)
        solve();
    return 0;
}